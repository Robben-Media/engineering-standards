# v1 initial rollout

Verification record for the first `v1` tag. Written 2026-08-28 (CDT).

## What shipped

| Field | Evidence |
| --- | --- |
| Tag | `refs/tags/v1` |
| Target SHA | `472bfa146993442c7ab32fe3920622a030905bec` |
| How confirmed | `GET /repos/Robben-Media/engineering-standards/git/matching-refs/tags` returned `{ref: refs/tags/v1, object.sha: 472bfa146993442c7ab32fe3920622a030905bec}` |
| `main` at the same SHA | `GET /repos/Robben-Media/engineering-standards/git/ref/heads/main` |
| Commit | [472bfa1](https://github.com/Robben-Media/engineering-standards/commit/472bfa146993442c7ab32fe3920622a030905bec) — `Add the class catalog and reusable CI workflows (#1)` (committer date 2026-08-28T18:23:25Z / 1:23 PM CDT) |

Contents of that commit (also current `main` when this record was drafted):

- Class catalog: `README.md`, `docs/classes.md`, `docs/labels.md`
- Reusable workflows: `go-cli.yml`, `node-bun.yml`, `python-tool.yml`, `docs-only.yml` (callable only; third-party actions referenced by mutable tags such as `@v4` / `@v5`)
- Gitignore templates under `templates/gitignore/`
- `LICENSE`

No in-repo caller, no fixtures, no release doc, no action SHA pins.

## What was not true at v1

- **Actions runs on this SHA:** `GET /repos/Robben-Media/engineering-standards/actions/runs` on 2026-08-28 returned `total_count: 2`. Both runs were on branch `harden-node-bun` (issue #3 / PR #7), not on `main` or tag `v1`. There is no observed Actions run of the reusable workflows at `472bfa1`.
- **Caller-repo runs:** not queried; this record does not claim any caller (`wpssh`, `fleet`, `meal-planning`, …) executed `@v1`.
- **Rulesets / branch protection:** `GET .../rulesets` was `[]`; `GET .../branches/main/protection` was `404 Branch not protected`; org rulesets `404` (missing `admin:org`). See [docs/release.md](../release.md).

## Rollback target

`472bfa146993442c7ab32fe3920622a030905bec` is the SHA to restore if a later `v1` move needs to be undone back to the initial catalog.

## Follow-up (this PR, not part of v1)

Release safeguards — in-repo Fixture CI, SHA-pinned third-party actions, `docs/release.md` — ship after this record, on a later SHA. Do not move `v1` to that SHA until Fixture CI is green and Nash or Jeremy reviews.
