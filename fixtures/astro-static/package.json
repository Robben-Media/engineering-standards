{
  "name": "fixture-astro-static",
  "private": true,
  "description": "Astro static profile fixture (detection probe; local stubs only, no registry packages)",
  "packageManager": "bun@1.4.0",
  "engines": {
    "node": ">=22.23.2 <23"
  },
  "scripts": {
    "test": "node --test",
    "ci": "node --test"
  },
  "dependencies": {
    "astro": "file:./vendor/astro"
  }
}
