// Local stub of `astro/config` for this fixture. It returns the config object
// unchanged so astro.config.mjs stays importable and its exported values can be
// asserted. It is not the real Astro implementation.
export function defineConfig(input) {
  return input;
}
